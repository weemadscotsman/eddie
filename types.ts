export interface ServiceItem {
  id: string;
  title: string;
}

export interface StepItem {
  id: string;
  title: string;
  description: string;
  details: string[];
}

export interface ListItem {
  id: string;
  text: string;
}